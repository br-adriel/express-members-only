<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="/css/reset.css" />
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/profile.css" />

    <%- include('../layout/favicon') %>

    <title><%= userProfile.username %> - PostHere!</title>
  </head>
  <body>
    <%- include('../layout/header') %>

    <main>
      <div class="container">
        <div class="profile card">
          <img src="<%=userProfile.profileImage || '/img/users/profile/default.jpg' %>" alt="">
          <div class="user-data">
            <h2>
              <%= userProfile.fullName %>
            </h2>
            <p><%= userProfile.username %></p>
            <% if (userProfile.hasMembership) { %>
              <p class="membership active">
                <i class="bi bi-gem"></i>
                VIP
              </p>
            <% } %>

          </div>
        </div>
        
        <% if (user && userProfile.username === user.username) { %>
          <a href="/users/<%=user.username%>/edit" class="btn edit-profile">
            <i class="bi bi-pen"></i>
          </a>
        <% } %>
      </div>
    </main>

    <%- include('../layout/footer') %>
  </body>
</html>
